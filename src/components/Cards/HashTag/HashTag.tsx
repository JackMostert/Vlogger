import * as React from "react";

interface IHashTagProps {}

const HashTag: React.FunctionComponent<IHashTagProps> = (props) => {
  return <div className="hashtag-root">HashTag</div>;
};

export default HashTag;
